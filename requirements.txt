# Calisma zamani Python bagimliliklari (Raspberry Pi 5 + gercek donanim).
# Kurulum:  pip install -r requirements.txt
#
# NOT: rospy (ROS) ve cv2'nin sim tarafi YALNIZ simulasyon icindir; gercek
# donanimda ROS KURULMAZ (bkz. DONANIM_GOREV.md). Asagidakiler hem sim hem
# donanim icin ortak calisma zamani paketleridir.

pymavlink>=2.4.49      # MAVLink: gudum komutu (SET_POSITION_TARGET) + telemetri okuma
redis>=4.5.0           # surecler arasi kanal (tracker_bbox, komut_yetkisi, ...)
numpy>=1.21            # MPC cozucu (FISTA), filtreleme, geometri
filterpy>=1.4.5        # IMM/Kalman hedef durum filtreleri
scipy>=1.7             # FilterPy sayisal lineer cebir islemleri

# --- Yalniz belirli araclar/senaryolar icin ---
opencv-python>=4.5     # bbox_to_redis HSV tespiti (SIM). Donanimda IMX500 NN
                       # dedektoru bunun yerini alir (bkz. DONANIM_GOREV Gorev 2).
matplotlib>=3.5        # tools/uc_boyut_video.py (CEVRIMDISI 3B izleme videosu)

# --- Donanim tespit katmani (Pi 5, apt ile kurulur, pip DEGIL) ---
#   sudo apt install python3-picamera2 imx500-all
# picamera2 / IMX500 firmware apt paketleridir; buraya pip ile yazilmaz.

# Tekrarlanabilir ve CI ile dogrulanan ortam icin:
#   python3 -m pip install -r requirements-lock.txt
